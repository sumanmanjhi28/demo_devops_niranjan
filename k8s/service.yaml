apiVersion: v1
kind: Service
metadata:
  name: demo-devops-service
  namespace: default
  labels:
    app: demo-devops
spec:
  type: LoadBalancer
  selector:
    app: demo-devops
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  sessionAffinity: None
